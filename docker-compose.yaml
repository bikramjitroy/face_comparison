version: '2.3'

services:
  face_comparison:
    image: face_comparison
    container_name: face_comparison
    working_dir: /root
    build:
      context: .
      #Uncomment this line to run the example on the GPU (requires Nvidia-Docker)
      # dockerfile: Dockerfile.gpu 
    ports:
            - "8501:8501"
    volumes:
      - /dacx/uploads:/root/uploads

